# Railway Environment Variables
# This file is used during the Railway build process

# For Railway deployments, we use relative paths for API calls
# The actual API URL should be set in Railway environment variables
VITE_API_URL=/api