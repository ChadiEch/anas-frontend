# Railway Environment Variables
# This file is used during the Railway build process

# For Railway deployments, we use the internal Railway URL for API calls
VITE_API_URL=https://anas-backend.railway.internal/api